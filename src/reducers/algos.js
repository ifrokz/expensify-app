// Expenses reducer
const algosReducerDefaultState = [];
const algosReducer = (state = algosReducerDefaultState, action) => {
    switch (action.type){
        default:
            return state;
    };
};

export default algosReducer;